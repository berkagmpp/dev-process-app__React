import React from "react";



import Button from "../UI/Button"


const SkateholderButton = props => {


    return (
        <Button onClick={props.onClick}>Stakeholder &amp; feasibility study</Button>
    );
};

export default SkateholderButton;